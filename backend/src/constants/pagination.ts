/**
 * Pagination defaults and bounds shared by every list endpoint.
 */
export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_SIZE = 10;
export const MIN_PAGE_SIZE = 1;
export const MAX_PAGE_SIZE = 100;

/** Number of items returned in "recent" dashboard lists. */
export const DASHBOARD_RECENT_LIMIT = 5;

/** Number of recommended jobs returned on the candidate dashboard. */
export const RECOMMENDED_JOBS_LIMIT = 5;
