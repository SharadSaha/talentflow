/**
 * Database seed script.
 *
 * Placeholder only — there are no models to seed yet. Once the Prisma schema
 * defines models (e.g. User, Job), import the generated client here and insert
 * seed data (default HR / Candidate accounts, sample jobs, etc.).
 *
 * Run with: `npm run prisma:seed`
 */
async function main(): Promise<void> {
  console.log('🌱 Seed placeholder — no models defined yet. Nothing to seed.');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
