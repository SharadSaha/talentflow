/**
 * Semantic intent shared by status badges and other status indicators. Maps to
 * the design system's semantic colour tokens.
 */
export type BadgeIntent = 'neutral' | 'primary' | 'success' | 'warning' | 'danger' | 'info';
